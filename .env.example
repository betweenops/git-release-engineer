RELEASE_DEFINITION_ROOT=/path/to/release-definition-repo
# Optional explicit overrides if you do not want to derive from RELEASE_DEFINITION_ROOT
# RELEASE_MANIFEST_PATH=/path/to/release-definition-repo/release.yaml
# RELEASE_VARS_FILE=/path/to/release-definition-repo/release.vars.env

GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=replace-with-read-only-or-minimum-scope-token
