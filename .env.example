EDGE_RELEASE_ROOT=/path/to/edge-release
# Optional explicit overrides if you do not want to derive from EDGE_RELEASE_ROOT
# EDGE_RELEASE_MANIFEST=/path/to/edge-release/release.yaml
# EDGE_RELEASE_VARS_FILE=/path/to/edge-release/release.vars.env

GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=replace-with-read-only-or-minimum-scope-token
